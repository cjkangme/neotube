extends ../base

block content
    img(src=`${user.avatarUrl}` width="100" height="100")
    div
        small 이메일
        span=user.email
    div
        small 유저명
        span=user.username
    div
        small 지역
        span=user.location